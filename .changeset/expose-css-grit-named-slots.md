---
"@biomejs/biome": minor
---

Added CSS Grit named slots for selectors and declarations. `biome search` patterns can now match selector and property names through fields like `name`, `ident`, and `property`.
